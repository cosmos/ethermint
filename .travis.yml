language: go

go:
  - 1.13.x

cache:
  directories:
    - "$HOME/.cache/go-build"
    - "$GOPATH/pkg/mod"

matrix:
  include:
    - name: Tests
      script:
        - make test-unit
        - make test-import
        - make test-race
    - name: Verify deps & Lint
      script:
        - make verify build
        - make lint
